async function run() {
  const url = "https://tonapi.io/v2/nfts/search?limit=10&query=Telegram%20Gift"; // failed before with 'can't decode address search' because query is not supported?
  
  // Let's get the collection by searching dns?
  // Let's just fetch random items and see?
}
run();
